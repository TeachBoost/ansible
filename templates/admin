<!DOCTYPE html>
<html>
<head>
    <title>Ansible Admin</title>
</head>
<body>
    <h1>Ansible Admin</h1>

    <h3>User list</h3>
    <form method="POST" action="admin/delete">
        <table>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Delete</th>
            </tr>
            % for user in users:
                <tr>
                    <td><a href="admin/{{user.id}}">{{user.name}}</a></td>
                    <td>{{user.email}}</td>
                    <td><input name="{{user.id}}" type="checkbox" /></td>
                </tr>
            % end
            <tr>
                <td /><td /><td><button type="submit">Delete Selected</button></td>
        </table>
    </form>
    <h3>Create new user</h3>
    <form method="POST" action="admin/add">
        <label for="name">Name: </label>
        <input name="name" />
        <label for="email">Email: </label>
        <input name="email" type="email" />
        <button type="submit">Create</button>
    </form>
</body>
</html>