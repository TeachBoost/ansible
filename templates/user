<!DOCTYPE html>
<html>
<head>
    <title>Ansible - {{user.name}}'s Settings</title>
</head>
<body>
    <h1>Settings for {{user.name}}</h1>
    <a href="{{home}}/admin">Back to admin</a>
    <form method="POST">
        <table>
            <tr>
                <th>Day</th>
                <th>Time</th>
            </tr>
            % for day in ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun']:
                <tr>
                    <td>{{day}}</td>
                    <td><input style="width:40px" name="{{day}}" value="{{getattr(user, day) if getattr(user,day) else ''}}" /></td>
                </tr>
            % end
        </table>
        <button type="submit">Save</button>
    </form>
</body>
</html>
